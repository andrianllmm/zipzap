# ZipZap

A text file compressor built from scratch using canonical Huffman coding and custom data structures.

## Usage

Compress a text file:

```bash
zipzap zip input.txt -o output.zz
```

Decompress a compressed text file:

```bash
zipzap zap input.zz -o output.txt
```
